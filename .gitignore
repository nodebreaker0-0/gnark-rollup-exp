# build / proving artifacts (regenerable)
*.r1cs
*.pk
*.vk
*.proof
*.ccs
input.public
/circuit.r1cs.pk

# Go
bin/
dist/
*.test
*.out

# scratch / probe files (cannot be unlinked on this FS; ignore them)
_probe*.txt

# editor/linter backup artifacts (e.g. foo.go.123456789) left on a no-unlink FS
*.go.[0-9]*
